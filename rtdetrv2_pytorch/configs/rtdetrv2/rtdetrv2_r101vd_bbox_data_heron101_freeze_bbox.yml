__include__: [
  './rtdetrv2_r101vd_bbox_data_heron101.yml',
]

# Head-scope experiment: freeze bbox heads while keeping classification heads
# and the shared body trainable.

output_dir: ./output/rtdetrv2_r101vd_bbox_data_heron101_freeze_bbox
PResNet:
  pretrained: False
find_unused_parameters: True
freeze_mode: freeze_bbox
