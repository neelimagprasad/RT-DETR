__include__: [
  './rtdetrv2_r101vd_bbox_data_heron101.yml',
]

# Full fine-tune baseline that keeps the stronger fine-tuning LR recipe while
# matching the longer warmup used in the stronger multi-GPU run recipe.

output_dir: ./output/rtdetrv2_r101vd_bbox_data_heron101_strong_lr_warmup100

PResNet:
  pretrained: False

lr_warmup_scheduler:
  warmup_duration: 100
