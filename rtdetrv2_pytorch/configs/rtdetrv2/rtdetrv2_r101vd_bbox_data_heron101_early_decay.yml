__include__: [
  './rtdetrv2_r101vd_bbox_data_heron101.yml',
]

# Baseline-control experiment: keep the winning 640 recipe, but decay earlier
# to test whether locking in gains sooner improves the peak validation AP.

output_dir: ./output/rtdetrv2_r101vd_bbox_data_heron101_early_decay

lr_scheduler:
  type: MultiStepLR
  milestones: [30, 60, 90]
  gamma: 0.2
